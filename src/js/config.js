(function (App) {
  window.App = App

  App.config = {

    // configure the server scheme, domain and (if necessary) port; please keep
    // the trailing slash:
    serverUrl: 'https://locahost:8000/'

  }
})(window.App || {})
